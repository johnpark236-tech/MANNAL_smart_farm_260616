# Frontend

React + Vite 기반 대시보드입니다.

## 실행

```bash
npm install
npm run dev
```

기본 백엔드 주소는 `http://localhost:8000`입니다. 변경하려면 `frontend/.env`에 아래 값을 추가합니다.

```text
VITE_API_BASE_URL=http://localhost:8000
```
